FROM python:3.12-slim

RUN pip install --no-cache-dir \
    paho-mqtt \
    websockets \
    aiohttp \
    pytest \
    pytest-asyncio

WORKDIR /tests

CMD ["python", "-m", "pytest", "/tests", "-v", "--tb=short", "-s"]
